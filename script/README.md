    Resolve the target directory safely across:
    - Windows
    - WSL (Linux accessing Windows via /mnt/c)
    - Native Linux

    Default behavior:
    Use the directory where this script is located.
    